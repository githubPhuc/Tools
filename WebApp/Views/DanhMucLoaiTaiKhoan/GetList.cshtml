
@{
    ViewBag.Title = "Danh sách";
    var data = ViewBag.data as List<ToolsApp.EntityFramework.Config>;
}
<table id="myTable" class="table table-bordered table-striped">
    <thead>
        <tr>
            <th></th>
            <th>Tên loại tài khoản (Vi)</th>
            <th>Tên loại tài khoản (Eng)</th>
            <th>Tình trạng</th>
        </tr>
    </thead>
    <tbody>
        @{ var STT = 1; }
        @{
            if (data != null)
            {
                foreach (var item in data)
                {
                    <tr>
                        <td>
                            <a onclick="Edit('@item.Id')" class="btn btn-secondary btn-custom">
                                <i class="fas fa-edit"></i> Sửa
                            </a>
                            <a onclick="Delete('@item.Id')" class="btn btn-danger btn-custom">
                                <i class="far fa-trash-alt"></i> Xóa
                            </a>
                        </td>
                        <td>@item.MoTa</td>
                        <td>@item.moTaChiTiet</td>
                        <td>                           
                                                @if (@item.hieuLuc == true)
                                                {
                                                    <div class="d-flex align-items-center">
                                                        <i class="fa-solid fa-circle" style="color: green;"></i>
                                                        <span class="ml-1">Đang hoạt động</span>
                                                    </div>
                                                }
                                                else
                                                {
                                                    <div class="d-flex align-items-center">
                                                        <i class="fa-solid fa-circle" style="color: red;"></i>
                                                        <span class="ml-1">Không hoạt động</span>
                                                    </div>
                                                }
                       </td>
                    </tr>

                    STT++;
                }
            }
           
        }

        </tbody>
    </table>
